{
  "name": "@janatha/backend",
  "version": "2.0.0",
  "type": "module",
  "main": "src/app.ts",
  "types": "src/types.ts",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "deploy": "wrangler deploy --config wrangler.toml",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:cloudflare": "vitest run",
    "test:cloudflare:app": "vitest run src/__tests__/app.test.ts",
    "test:watch": "vitest"
  },
  "dependencies": {
    "hono": "^4.7.0",
    "jose": "^6.0.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.13.0",
    "@cloudflare/workers-types": "^4.20260101.0",
    "typescript": "~5.9.2",
    "vitest": "^4.1.0"
  }
}
