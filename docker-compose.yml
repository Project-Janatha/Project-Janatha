version: '3.8'
services:
  app:
    build: .
    ports:
      - "80:80"      # Nginx serving frontend
      - "8008:8008"  # Backend API
    env_file:
      - ./packages/backend/.env
    environment:
      - PORT=8008